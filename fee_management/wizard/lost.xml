<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="lost_cancel_form_view" model="ir.ui.view">
        <field name="name">lost.Reason.form</field>
        <field name="model">lost.fee</field>
        <field name="arch" type="xml">
            <form string=" Fee Cancel Reason">
                <group>
                    <field name="cancel_id"/>
<!--                    <field name="cancel_date"/>-->
                    <field name="cancel_person_id"/>
                    <field name="admit_date"/>
                </group>
                <field name="reason" placeholder="?? what happened"/>
                <footer>
                    <button name="fee_cancel" string="Submit" type="object" class="oe_highlight"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="fee_reason_action" model="ir.actions.act_window">
        <field name="name">cancel Reason</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">lost.fee</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="lost_cancel_form_view"/>
        <field name="target">new</field>

    </record>

</odoo>